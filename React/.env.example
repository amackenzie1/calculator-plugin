# Example environment variables file (copy to .env.local for development)

# API Gateway endpoint for data logging
# This is the URL from your CDK deployment output
VITE_API_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod
EOL < /dev/null